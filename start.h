#ifndef _START_H /* L'écriture ici présente n'est pas universelle */
#define _START_H /* Edit suite aux corrections des posts suivants -> */

#include "start.h"

/* On reserve le 0 pour les retours aux menu */
#define DEBUT 1 /* On lance la fonction theme */
#define OPTIONS 2 /* On lance la fonction Options */

struct renvoie start(void);

#endif