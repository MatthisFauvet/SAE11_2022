#ifndef _DIFF_H /* L'écriture ici présente n'est pas universelle */
#define _DIFF_H /* Edit suite aux corrections des posts suivants -> */

#include "diff.h"

/* On reserve le 0 pour les retours aux menu */

struct renvoie diff(void);

#endif